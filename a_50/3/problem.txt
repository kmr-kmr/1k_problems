Given input an array[] of n integers, every element is repated twice except one
, find it.

Note: You can be certain that only one number not repeated.

Example:
   input = [1, 3, 4, 6, 2, 1, 4, 6, 2]
   output = 3  (As 3 appeared only once, others are repeated twice)